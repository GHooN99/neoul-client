import { pallete } from "../pallete";

export const buttons = {
  main: pallete.blue,
  gray: pallete.gray[400],
  white: pallete.gray[200],
  red: pallete.red,
};

export type Buttons = typeof buttons;
